# Viva Final Deployment Fix - May 19
name: Trio Cafe CI/CD Pipeline

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # --- Backend Setup ---
      - name: Setup Node.js (Backend)
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: backend/package-lock.json

      - name: Install Backend Dependencies
        run: npm ci
        working-directory: ./backend

      # --- AUTOMATED TESTING STAGE (Task B1.2) ---
      - name: Set up Python for Testing
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install Selenium and Webdriver Manager
        run: |
          pip install selenium webdriver-manager

      - name: Run Selenium Tests
        run: python selenium_tests.py

      # --- Docker Preparation ---
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # --- Build & Push Backend (Task B1.3) ---
      - name: Build & Push Backend Image
        uses: docker/build-push-action@v6
        with:
          context: ./backend
          file: ./backend/Dockerfile
          push: true
          tags: |
            laibaafzaal98/trio-cafe-backend:latest
            laibaafzaal98/trio-cafe-backend:${{ github.sha }}

      # --- Build & Push Frontend (Task B1.3) ---
      - name: Build & Push Frontend Image
        uses: docker/build-push-action@v6
        with:
          context: ./frontend
          file: ./frontend/Dockerfile
          push: true
          tags: |
            laibaafzaal98/trio-cafe-frontend:latest
            laibaafzaal98/trio-cafe-frontend:${{ github.sha }}

      # --- STAGE 4: DEPLOYMENT TO KUBERNETES (Task B1.4) ---
      - name: Set Kubernetes Context
        uses: azure/k8s-set-context@v3
        with:
          method: kubeconfig
          kubeconfig: ${{ secrets.KUBECONFIG }}

      - name: Deploy to AKS
        uses: azure/k8s-deploy@v4
        with:
          namespace: trio-cafe
          manifests: |
            k8s/namespace.yaml
            k8s/mongodb-deployment.yaml
            k8s/backend-deployment.yaml
            k8s/frontend-deployment.yaml
          images: |
            laibaafzaal98/trio-cafe-backend:${{ github.sha }}
            laibaafzaal98/trio-cafe-frontend:${{ github.sha }}